# --- Chatbot Vocal : dépendances backend (Python 3.10+) ---
# L'application requiert aussi le binaire système « ffmpeg » (utilisé par pydub).
Flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.8.0
flask-sock==0.7.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.35
PyJWT==2.9.0
python-docx==1.1.2
pydub==0.25.1
reportlab==4.2.2
simple-websocket==1.1.0
vosk==0.3.45

# Serveur WSGI asynchrone (requis pour les WebSockets en production)
gevent==24.2.1
gevent-websocket==0.10.1
gunicorn==22.0.0

# Moteur Whisper optionnel (non installé par défaut, voir requirements-whisper.txt)
